<%= turbo_frame_tag "availability" do %>
  <div class="flex justify-center w-full">
    <div id="view_availability" class="w-full">
      <%  has_availabilities = user.has_availabilities?  %>
      <div class="flex justify-between w-full">
        <div class="flex-1">
          <h2 class="pb-4 leading-3 roboto-font font-medium text-xs">Availability</h2>
          <% unless has_availabilities %>
            <p class="leading-2 roboto-font font-light text-sm">When are you available to clients?</p>
          <% end %>
        </div>
        <div class="flex items-start">
        <% if editable %>
          <%= link_to "Edit", profile_edit_path("availability"), class: "text-blue-500 text-sm" %>
        <% end %>
        </div>
      </div>
      <% if has_availabilities %>
        <ul class="w-full" style="max-width: 330px">
          <% user.availabilities.each do |availability| %>
            <li class="flex justify-between w-full py-4 border-t">
              <span class="roboto-font font-light text-sm">
                <%= availability.day_name %>
              </span>
              <span class="text-right roboto-font font-light text-sm">
                <% if availability.opened? %>
                  <%= "#{availability.open_at_in_text} - #{availability.close_at_in_text}" %>
                <% else %>
                  Not Available
                <% end %>
              </span>
            </li>
          <% end %>
        </ul>
      <% end %>
    </div>
  </div>
<% end %>
