<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>
  <body class="bg-gray-200">
    <header class="w-full bg-white border-b md:h-20 dark:bg-gray-900 dark:border-gray-700" x-data="{ mobileMenuOpen : false }">
      <nav class="px-4 py-3 mx-auto space-y-4 max-w-screen-2xl md:flex md:justify-between md:items-center md:py-5 md:space-y-0 md:space-x-10">
        <%= render "shared/logo" %>
      </nav>
    </header>
    <div class="font-sans bg-gray-100 antialiase md:pb-5 h-screen-headless md:pt-5">
      <div class="flex flex-col justify-center w-screen p-4 space-y-4 bg-white shadow sm:m-auto sm:w-96 h-screen-mobile ">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
