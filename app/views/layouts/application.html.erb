<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>
  <body class="md:overflow-hidden">
    <div class="md:min-h-screen">
      <div class="md:flex md:justify-center">
        <%= render "shared/header" %>
        <main class="flex-1 md:mt-20 max-w-screen-2xl mx-auto">
          <%= yield %>
        </main>
        <%= render "shared/footer" %>
      </div>
    </div>
  </body>
</html>
