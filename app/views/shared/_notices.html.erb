<% flash.each do |msg_type, message| %>
  <% next if msg_type.to_s == "timedout" || message.blank?  %>
  <div class="alert <%= alert_class_for(msg_type) %>">
    <div class="container">
      <button class="close" data-dismiss="alert"><span>×</span></button>
      <%= message %>
    </div>
  </div>
<% end %>
